# Options

The content on this page has been split up into [Formatting Options](@ref formatting-options) and [File Options](@ref file-options).

File options refer to those which can only be used in [`.JuliaFormatter.toml` configuration files](@ref config) (they control the overall behaviour of JuliaFormatter when run on a filesystem).
